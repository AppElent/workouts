One-line: an empty region that explains itself and offers the next step.

```jsx
<EmptyState
  title="No routines yet"
  body="A routine is a template — the exercises, sets and reps you repeat."
  action={{ label: "New routine", onPress: create }}
/>
```

`appearance="search"` for "nothing matches" inside a filtered list.
