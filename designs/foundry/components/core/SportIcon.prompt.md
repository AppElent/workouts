One-line: per-activity-type tile — tinted background, coloured letter glyph.

```jsx
<SportIcon sport="running" size={40} />
```

Sports are a closed set: strength, running, cycling, wod. Sizes in use: 24 (quick links), 32 (picker tiles), 40 (list rows).
