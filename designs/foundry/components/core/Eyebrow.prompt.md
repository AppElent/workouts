One-line: the uppercase micro-heading that names a section on the shell screens.

```jsx
<Eyebrow>Routines</Eyebrow>
```

Pair it with a right-aligned accent text action ("New", "See all") inside a space-between row.
