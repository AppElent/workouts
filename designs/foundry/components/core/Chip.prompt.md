One-line: a 34px pill for filters, set types and tags.

```jsx
<Chip label="Chest" active onClick={() => setFilter("chest")} />
<Chip label="barbell" />
```

Rows of chips scroll horizontally with 6–7px gaps. Active = lime fill with near-black ink.
