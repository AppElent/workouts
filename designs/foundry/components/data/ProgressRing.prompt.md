One-line: a ring for a bounded count — sessions this week, energy against a goal.

```jsx
<ProgressRing value={3} target={8} size={56} label="3 of 8 sessions this week" />
```

One series only; the multi-segment macro donut is not part of this system. Under 48px the inner number stops being legible — use a linear track instead.
