One-line: the month grid behind the diary's date control.

```jsx
<NutritionCalendar monthLabel="September 2026" offset={1} days={30} selected={17} today={17} onSelect={pick} />
```

Selected = lime filled circle; today = lime ring. Both states are also announced, never colour-only.
