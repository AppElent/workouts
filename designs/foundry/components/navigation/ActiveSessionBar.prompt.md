One-line: the pill that says a workout is still running and takes you back to it.

```jsx
<ActiveSessionBar name="Push day" elapsed="12:04" onPress={resume} />
```

Hidden on the session screen itself. It occupies layout space — it must never float over the tab bar or a screen's last control.
