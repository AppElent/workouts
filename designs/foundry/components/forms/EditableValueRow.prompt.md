One-line: a repeated saved object as one 60px row — tap to edit, optional visible Delete.

```jsx
<EditableValueRow label="Bench press" value="4 × 8 · 60 kg" deleteLabel="Delete" onPress={edit} onDelete={remove} />
```

The visible Delete is required whenever deletion also lives in a swipe or long-press menu.
