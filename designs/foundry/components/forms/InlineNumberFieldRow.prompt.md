One-line: one-line numeric row — the default for macros, weights and targets.

```jsx
<InlineNumberFieldRow label="Weight" value="82.5" suffix="kg" />
```

The field is 82px wide, right-aligned, tabular. Keep the label short so it survives Dynamic Type XL.
