One-line: the bare text action — Cancel, Save, Delete, "See all".

```jsx
<TextAction label="Cancel" tone="neutral" onClick={close} />
<TextAction label="Delete entry" tone="destructive" onClick={remove} />
```

Accent is the default tone. Labels are short verbs; the screen title supplies the object.
