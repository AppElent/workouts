One-line: the lime capsule — one per screen, the thing the screen is for.

```jsx
<PrimaryButton label="Log set 3" onClick={log} />
<PrimaryButton label="Start activity" size="lg" />
```

Press feedback is a colour change to `--accent-pressed`, never a fade. Don't use it for Cancel, Delete, navigation or disclosure — those are TextAction or a row.
