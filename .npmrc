# Registry mapping for the private @appelent scope. Authentication stays in the
# trusted user-level config; the EAS pre-install hook writes it there from the
# NODE_AUTH_TOKEN secret before dependencies are installed.
@appelent:registry=https://npm.pkg.github.com
